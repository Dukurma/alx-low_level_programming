#include "main.h"
/**
 * _abs - compute an absolute value of an integer.
 * @n: the integer to be conputed
 *
 * Return: the absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
